# Code Review for app-core (Tâche 02)

Modèles abstraits BaseModel et ActivityPubMixin + utilitaires communs.

- Statut: Terminé
- Confidence: 🟢 Aucun problème critique

## Main expected Changes

- [x] `suddenly/core/models.py` — BaseModel (UUID PK, timestamps, abstract)
- [x] `suddenly/core/mixins.py` — ActivityPubMixin (AP fields, db_index, abstract)
- [x] `suddenly/core/utils.py` — generate_unique_slug (type hints complets)

## Scoring

| Title | Files | Score |
|-------|-------|-------|
| `get_ap_id()` appelle `self.get_absolute_url()` non défini sur le mixin — pas de contrat explicite | `mixins.py:58` | 1 |
| `generate_unique_slug` suppose un champ `slug` sur le modèle — pas de vérification ni docstring d'avertissement | `utils.py:32` | 1 |
| `private_key` stocké en clair en DB (TextField) — risque si la DB est compromise | `mixins.py:40` | 1 |

## ✅ Code Quality Checklist

### Potentially Unnecessary Elements

- [🟢] Aucun import mort (`from typing import Any` correctement éliminé)

### Standards Compliance

- [🟢] Naming conventions : snake_case, booléen `local` (préfixe `is_` non requis sur un champ Django)
- [🟢] Type hints : présents sur toutes les méthodes publiques
- [🟢] Docstrings : présentes sur toutes les classes et méthodes publiques
- [🟢] `abstract = True` sur les deux modèles

### Architecture

- [🟢] Séparation correcte models / mixins / utils
- [🟢] BaseModel sans logique métier — pur scaffold
- [🟡] **Contrat `get_absolute_url()` implicite** `mixins.py:58` — `ActivityPubMixin.get_ap_id()` appelle `self.get_absolute_url()` qui doit être implémenté par les classes concrètes. Ce contrat n'est pas documenté dans la docstring. (Ajouter une note dans la docstring)

### Code Health

- [🟢] Taille : models.py 30 lignes, mixins.py 63 lignes, utils.py 37 lignes — toutes < 50 lignes
- [🟢] Complexité cyclomatique : faible (1-2 branches max)
- [🟢] `__repr__` présent sur BaseModel
- [🟡] **Champ `slug` assumé** `utils.py:32` — `generate_unique_slug` filtre sur `slug=slug` mais le modèle passé peut ne pas avoir ce champ. En runtime Django, ça lève une FieldError explicite, donc Fail Fast respecté — mais la docstring devrait mentionner ce prérequis.

### Security

- [🟢] Aucune donnée externe non validée
- [🟢] Aucun secret en dur
- [🟡] **`private_key` en clair** `mixins.py:40` — le champ help_text dit "chiffrée" mais c'est un TextField sans chiffrement applicatif. Le chiffrement est à la charge des classes concrètes. Acceptable pour un mixin abstrait, mais le help_text est trompeur si aucun chiffrement n'est implémenté en amont. (Garder tel quel, à traiter lors de l'implémentation des clés)

### Error management

- [🟢] `generate_unique_slug` : pas de gestion d'erreur nécessaire — Django lève FieldError explicitement si `slug` absent (Fail Fast)

### Performance

- [🟢] `generate_unique_slug` : potentiellement N requêtes en cas de collisions répétées — acceptable car les slugs sont uniques par conception

### Backend specific

#### Logging

- [🟢] Pas de logging nécessaire sur des modèles abstraits

## Final Review

- **Score**: 🟢 Mineur — 3 points score 1, aucun score 2 ou 3
- **Feedback**: Implémentation propre, conforme au plan et aux règles. Les 3 points sont des améliorations de documentation, pas des bugs.
- **Follow-up Actions**:
  - Ajouter dans la docstring de `get_ap_id()` : "Les classes concrètes doivent implémenter `get_absolute_url()`"
  - Ajouter dans la docstring de `generate_unique_slug` : "Le modèle doit posséder un champ `slug`"
  - `private_key` : traiter le chiffrement lors de l'implémentation des clés AP (tâche future)
- **Additional Notes**: Les points score 1 sont tous documentaires. Aucun fix de code requis pour la validité fonctionnelle.
